import type { Agent } from '@/types';

// Mock logged-in agent
export const MOCK_AGENT_ID = 'agent123';

export const mockAgent: Agent = {
  id: MOCK_AGENT_ID,
  name: 'John Doe (Agent)',
  email: 'john.doe@properview.com',
};
